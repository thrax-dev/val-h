import AboutMe from "../AboutMe";

const Main: React.FC = () => {
  return (
    <main className="w-full px-3 md:px-10 flex justify-center md:block">
      <AboutMe />
    </main>
  );
};

export default Main;
