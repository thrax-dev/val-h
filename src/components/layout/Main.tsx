import AboutMe from "../AboutMe";

const Main: React.FC = () => {
  return (
    <main className="w-screen px-3 md:px-10 flex items-center justify-center md:block">
      <AboutMe />
    </main>
  );
};

export default Main;
