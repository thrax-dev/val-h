name: Hostinger Continuous Deployment

on:
  push:
    branches: [main]
    paths-ignore:
      - "components"
      - "pages"

  workflow_dispatch:

jobs:
  build:
    name: Build Job
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@main

      - name: 📦 Install packages
        run: npm ci

      - name: 🔨 Build App
        run: npm run build

      - name: Archieve production artifact
        uses: actions/upload-artifact@main
        with:
          name: dist
          path: dist

  deploy:
    name: Deploy Job
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@main

      - name: Checks before artifact
        run: ls -al

        # try to remove all folders and files except the dist artifact

      - name: Download artifact
        uses: actions/download-artifact@main
        with:
          name: dist
          path: dist

      - name: Checks before file uploads
        run: pwd && ls -al && cd dist/ && ls -al

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@master
        with:
          server: ${{ secrets.ftp_server }}
          username: ${{ secrets.ftp_username }}
          password: ${{ secrets.ftp_password }}
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            ./src/components/
            ./src/pages/


# Investigate - it works, but id doesn't work ???
